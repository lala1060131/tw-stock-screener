services:
  - type: web
    name: tw-stock-screener
    runtime: node
    buildCommand: pip3 install --no-cache-dir -r requirements.txt
    startCommand: node server.cjs
    plan: free
    envVars:
      - key: NODE_VERSION
        value: "18.18.0"
      - key: PORT
        value: "10000"
